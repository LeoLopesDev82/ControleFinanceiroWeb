namespace ControleFinanceiroWeb.Models.Entities
{
    // Enum representing the type of category entry (Fixed or Variable).
    public enum CategoryType
    {
        Fixed = 'F',
        Variable = 'V'
    }
}
